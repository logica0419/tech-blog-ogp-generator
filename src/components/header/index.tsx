import { FC } from "react";

import "./index.scss";

const index: FC = () => {
  return (
    <header className="header-container">
      <h1>OGP Generator</h1>
    </header>
  );
};

export default index;
